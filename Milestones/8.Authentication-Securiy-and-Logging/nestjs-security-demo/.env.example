# Environment Configuration
NODE_ENV=development
PORT=3000

# CORS Configuration - Comma-separated list of allowed origins
# Must be full URLs with protocol (http:// or https://)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000

# API Key for secured endpoints
# IMPORTANT: Must be at least 32 characters for security
# In production, use a secret manager instead of .env files
API_KEY=demo-api-key-32-chars-minimum-length-required-for-security
