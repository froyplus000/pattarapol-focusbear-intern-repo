meta {
  name: Create User - Email Conflict
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/users
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "John Doe",
    "email": "john@example.com",
    "password": "secret123"
  }
}

tests {
  test("Status should be 409", function() {
    expect(res.status).to.equal(409);
  });
  
  test("Should return conflict error", function() {
    expect(res.body).to.have.property('message');
  });
}
